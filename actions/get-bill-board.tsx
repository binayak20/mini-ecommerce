import { billboardDate } from "@/const/data";
import { Billboard } from "@/types";

const getBillboard = async (id: string): Promise<Billboard> => {
  return billboardDate;
};

export default getBillboard;
